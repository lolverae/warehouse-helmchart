apiVersion: v1
kind: Service
metadata:
  name: {{ include "warehouse-app.fullname" . }}
  labels:
    {{- include "warehouse-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "warehouse-app.selectorLabels" . | nindent 4 }}
